<!-- The eval value here is a list of tuples, where each tuple has two elements:

First Element (4): Specifies the operation to perform on the relationship.

4: Add a record to the relationship (in this case, add base.group_user to implied_ids).
Other common operations:
3: Remove a record from the relationship.
6: Replace all existing records with new ones -->

